# Graphics

Source files and exported visuals used in project documentation belong here.

Keep editable source files when possible. Use clear names such as:

```text
architecture-overview.svg
message-validation-flow.svg
operator-dashboard-concept.png
```

Diagrams that must stay synchronized with text should preferably remain as Mermaid in the relevant Markdown file. Store static graphics here when Mermaid is not suitable.
